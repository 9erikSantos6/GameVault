<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GameVault - Gerenciar Amigo</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/amizade.css">
    <style>
        .bg-purple-sidebar { background-color: #5a009d !important; min-height: 100vh; min-width: 250px; }
        .nav-link:hover, .nav-link.active { background-color: rgba(255, 255, 255, 0.2) !important; }
    </style>
</head>
<body class="d-flex bg-light">

    <nav class="bg-purple-sidebar p-3 text-white shadow">
        <div class="fs-4 fw-bold mb-4 mt-2 px-2">🎮 GAMEVAULT</div>
        <ul class="nav nav-pills flex-column">
            <li class="nav-item"><a href="/dashboard" class="nav-link text-white">🏠 Dashboard</a></li>
            <li><a href="/amizade" class="nav-link active text-white">👥 Amigos</a></li>
            <li><a href="/emprestimos" class="nav-link text-white">🤝 Empréstimos</a></li>
        </ul>
    </nav>

    <main class="flex-grow-1 p-5">
        <h2 class="mb-4">Gerenciar Amizade</h2>
        <div class="card border-0 shadow-sm p-4 rounded-4" style="max-width: 600px;">
            <div class="text-center mb-4">
                <div class="display-4 mb-2">👤</div>
                <h4 class="fw-bold">@<%= amigo.nome %></h4>
                <p class="text-muted">@<%= amigo.email %></p>
            </div>
            <div class="alert alert-info border-0">
                Amigos desde: <strong><%= amizade.createdAt %></strong>
            </div>
            <hr>
            <div class="d-grid gap-2">
                <form action="/amizade/excluir/<%= amigo.id %>" method="POST">
                    <button type="submit" class="btn btn-danger w-100 py-2">Remover Amigo</button>
                </form>
                <a href="/amizade" class="btn btn-outline-secondary w-100">Voltar para a lista</a>
            </div>
        </div>
    </main>
</body>
</html>