<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>GameVault - Gerenciar Empréstimo</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/amizade.css">
    <link rel="stylesheet" href="/css/emprestimos.css">
</head>
<body class="bg-light">
    <div class="container-fluid p-0">
        <div class="row g-0">
            
            <nav class="col-md-2 bg-purple-sidebar p-3 text-white shadow" style="min-height: 100vh;">
                <div class="brand-font mb-4 mt-2 px-2 text-uppercase">🎮 GAMEVAULT</div>
                <ul class="nav nav-pills flex-column">
                    <li class="nav-item mb-2">
                        <a href="/dashboard" class="nav-link text-white">🏠 Dashboard</a>
                    </li>
                    <li class="nav-item mb-2">
                        <a href="/amizade" class="nav-link text-white">👥 Amigos</a>
                    </li>
                    <li class="nav-item mb-2">
                        <a href="/emprestimos" class="nav-link active text-white">🤝 Empréstimos</a>
                    </li>
                </ul>
            </nav>

            <main class="col-md-10 p-5 d-flex justify-content-center align-items-start">
                <div class="card border-0 shadow-sm p-5 rounded-4 text-center" style="max-width: 600px; width: 100%; background: white;">
                    <div class="display-4 mb-3">🔄</div>
                    <h4 class="fw-bold mb-4">O jogo já foi devolvido?</h4>
                    
                    <div class="card-resumo text-start mb-4 rounded-3 p-3">
                        <p class="mb-2"><strong>🎮 Jogo:</strong> <%= emprestimo.game.titulo %></p>
                        <p class="mb-2"><strong>👤 Com:</strong> @<%= emprestimo.mutuario.nome %></p>
                        <p class="mb-0 text-danger fw-bold small">📅 Prazo: <%= emprestimo.data_fim %></p>
                    </div>

                    <div class="d-grid gap-2">
                        <form action="/emprestimos/devolver/<%= emprestimo.id %>" method="POST">
                            <button type="submit" class="btn btn-success btn-lg w-100 fw-bold">Confirmar Devolução</button>
                        </form>
                        <a href="/emprestimos" class="btn btn-outline-secondary">Ainda não recebi</a>
                    </div>
                </div>
            </main>

        </div>
    </div>
</body>
</html>