<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>GameVault - Empréstimos</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/amizade.css">
    <link rel="stylesheet" href="/css/emprestimos.css">
</head>
<body>
    <div class="container-fluid p-0">
        <div class="row g-0">
            <nav class="col-md-2 bg-purple-sidebar p-3 text-white shadow">
                <div class="brand-font mb-4 mt-2 px-2 text-uppercase">🎮 GAMEVAULT</div>
                <ul class="nav nav-pills flex-column mb-auto">
                    <li class="nav-item"><a href="/dashboard" class="nav-link text-white">🏠 Dashboard</a></li>
                    <li><a href="/amizade" class="nav-link text-white">👥 Amigos</a></li>
                    <li><a href="/emprestimos" class="nav-link active text-white">🤝 Empréstimos</a></li>
                </ul>
            </nav>

            <main class="col-md-10 p-5 bg-light" style="min-height: 100vh;">
                <div class="row g-3 mb-4 text-center">
                    <div class="col-md-6">
                        <div class="card card-resumo shadow-sm">
                            <h2><%= emprestimosFeitos.length %></h2>
                            <p class="m-0 text-muted">Jogos Emprestados</p>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="card card-resumo shadow-sm" style="background-color: #d4edda;">
                            <h2 style="color: #155724;"><%= emprestimosRecebidos.length %></h2>
                            <p class="m-0 text-muted">Pegos com Amigos</p>
                        </div>
                    </div>
                </div>

                <div class="card border-0 shadow-sm p-4 rounded-4">
                    <h5 class="fw-bold mb-4">Jogos que Emprestei</h5>
                    <table class="table table-hover mt-2">
                        <thead>
                            <tr><th>Jogo</th><th>Com Quem Está</th><th>Data</th><th>Status</th></tr>
                        </thead>
                        <tbody>
                            <% emprestimosFeitos.forEach(e => { %>
                                <tr>
                                    <td class="fw-bold"><%= e.game.titulo %></td>
                                    <td>@<%= e.mutuario.nome %></td>
                                    <td><%= e.data_inicio %></td>
                                    <td>
                                        <span class="badge <%= e.retornou_em ? 'bg-secondary' : 'bg-warning text-dark' %>">
                                            <%= e.retornou_em ? 'Devolvido' : 'Emprestado' %>
                                        </span>
                                    </td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                    <a href="/emprestimos/novo" class="btn btn-primary mt-3" style="background-color: #5a009d; border: none;">Novo Empréstimo</a>
                </div>
            </main>
        </div>
    </div>
</body>
</html>